# python_multiprocess_log_test

This Repo test the logging module in multiprocess scenario
the baisc idea is put all the worker log into multiprocess.queue, 
then set a listener process to output the log
